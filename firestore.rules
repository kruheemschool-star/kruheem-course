rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write for now to fix permission issues
    // In production, we should lock this down more
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
