# -*- coding: utf-8 -*-
"""เขียนใหม่ข้อ 71-78 ของชุด "ระบบสมการเชิงเส้นสองตัวแปร" ม.3 — กราฟและจุดตัด"""
import sys, os
sys.path.insert(0, os.path.dirname(os.path.dirname(os.path.abspath(__file__))))
import sympy as sp
from kit import add, dump
from syskit import X, Y, T, S, N, P, holds, GRAPH

E = sp.Eq

# ── 71 ── ci=3
yc = sp.solve(sp.Eq(0 + Y, 6), Y)[0]; assert yc == 6
add(71, 3, r'กราฟของสมการ $x + y = 6$ ตัดแกน $Y$ ที่จุดใด',
    [r'$(6, 0)$', r'$(0, -6)$', P(0, yc), r'$(3, 3)$'], P(0, yc),
    [(1, 'สลับเป็นจุดตัดแกน X'), (2, 'ใส่เครื่องหมายลบเกิน'), (4, 'หารค่าคงตัวด้วยสอง')],
    r"""**คำตอบ: ข้อ 3.**

จุดที่กราฟตัดแกน $Y$ คือจุดที่อยู่บนแกน $Y$ พอดี และทุกจุดบนแกน $Y$ มีพิกัด $x$ เป็นศูนย์เสมอ ครูฮีมจึงแทน $x = 0$ ลงไป

$$0 + y = 6$$
$$y = 6$$

จุดตัดแกน $Y$ จึงเป็น $(0, 6)$

ทำคู่กันไปเลยจะได้เห็นภาพทั้งเส้น จุดตัดแกน $X$ หาได้จากการแทน $y = 0$
$$x + 0 = 6$$
$$x = 6$$
จุดตัดแกน $X$ คือ $(6, 0)$

สองจุดนี้กำหนดเส้นตรงได้ทั้งเส้นแล้ว ลากผ่าน $(0, 6)$ กับ $(6, 0)$ ก็ได้กราฟของสมการนี้

ครูฮีมย้ำว่าอย่าสับสนสองจุดนี้ ทั้งคู่มีเลข $6$ เหมือนกัน ต่างกันที่ตำแหน่งของศูนย์ ตัวหน้าเป็นศูนย์คือบนแกน $Y$ ตัวหลังเป็นศูนย์คือบนแกน $X$

**จำไว้: ตัดแกน $Y$ ให้แทน $x = 0$ ตัดแกน $X$ ให้แทน $y = 0$ ท่องว่าตัดแกนไหน ให้อีกตัวเป็นศูนย์ ไม่ใช่ตัวเดียวกับแกน**

**ทำไมตัวเลือกอื่นผิด:**
ตัวเลือก 1 ($(6, 0)$) ผิดเพราะนั่นคือจุดตัดแกน $X$ ไม่ใช่แกน $Y$ สลับตำแหน่งศูนย์
ตัวเลือก 2 ($(0, -6)$) ผิดเพราะใส่เครื่องหมายลบเกินมา ทั้งที่แทน $x = 0$ แล้วได้ $y$ เป็นบวก
ตัวเลือก 4 ($(3, 3)$) ผิดเพราะหารค่าคงตัวด้วยสองแล้วแบ่งให้สองแกนเท่ากัน ซึ่งไม่ใช่วิธีหาจุดตัดแกน""",
    T(GRAPH, 'เข้าใจ', 'ง่าย'))

# ── 72 ── ci=2
xc = sp.solve(sp.Eq(2 * X - 3 * 0, 12), X)[0]; assert xc == 6
add(72, 2, r'กราฟของสมการ $2x - 3y = 12$ ตัดแกน $X$ ที่จุดใด',
    [r'$(0, 6)$', P(xc, 0), r'$(-4, 0)$', r'$(0, -4)$'], P(xc, 0),
    [(1, 'สลับเป็นจุดตัดแกน Y'), (3, 'ใช้สัมประสิทธิ์ของ y มาหาร'), (4, 'ผิดทั้งแกนและตัวหาร')],
    r"""**คำตอบ: ข้อ 2.**

จุดตัดแกน $X$ คือจุดที่อยู่บนแกน $X$ พอดี ทุกจุดบนแกน $X$ มีพิกัด $y$ เป็นศูนย์ จึงแทน $y = 0$

$$2x - 3(0) = 12$$
$$2x = 12$$
$$x = 6$$

จุดตัดแกน $X$ จึงเป็น $(6, 0)$

หาจุดตัดแกน $Y$ ไว้เทียบด้วย แทน $x = 0$
$$2(0) - 3y = 12$$
$$-3y = 12$$
$$y = -4$$
จุดตัดแกน $Y$ คือ $(0, -4)$

สังเกตว่าตัวเลข $6$ กับ $-4$ มาจากคนละตัวหาร ตัวหนึ่งหารด้วยสัมประสิทธิ์ของ $x$ อีกตัวหารด้วยสัมประสิทธิ์ของ $y$ จับสลับกันเมื่อไหร่ผิดทันที

**เคล็ดของครูฮีม: หาจุดตัดสองแกนพร้อมกันเสมอ เขียนไว้ข้างโจทย์ทั้งคู่ แล้วค่อยเลือกตอบตามที่โจทย์ถาม เสียเวลาเพิ่มไม่ถึงครึ่งนาที แต่กันการสลับแกนได้หมดเลย**

**ทำไมตัวเลือกอื่นผิด:**
ตัวเลือก 1 ($(0, 6)$) ผิดเพราะเอาค่า $x$ ที่หาได้ไปวางในตำแหน่งของ $y$ สลับแกนกัน
ตัวเลือก 3 ($(-4, 0)$) ผิดเพราะเอาค่าที่ได้จากการหารด้วยสัมประสิทธิ์ของ $y$ มาวางเป็นพิกัด $x$
ตัวเลือก 4 ($(0, -4)$) ผิดเพราะนั่นคือจุดตัดแกน $Y$ ไม่ใช่แกน $X$ ตามที่โจทย์ถาม""",
    T(GRAPH, 'เข้าใจ', 'กลาง'))

# ── 73 ── ci=3
a, b = S(E(Y, X + 2), E(Y, -X + 8)); assert (a, b) == (3, 5)
add(73, 3, r'จุดตัดของกราฟ $y = x + 2$ และ $y = -x + 8$ คือจุดใด',
    [r'$(5, 3)$', r'$(2, 4)$', P(a, b), r'$(4, 6)$'], P(a, b),
    [(1, 'สลับพิกัด x กับ y'), (2, 'จับสองสมการเท่ากันแล้วย้ายข้างพลาด'), (4, 'ไม่ตรวจกับสมการที่สอง')],
    r"""**คำตอบ: ข้อ 3.**

จุดตัดของกราฟสองเส้น คือจุดที่อยู่บนทั้งสองเส้นพร้อมกัน แปลว่าที่จุดนั้นค่า $y$ ของทั้งสองสมการต้องเท่ากัน ครูฮีมจึงจับฝั่งขวาของทั้งสองมาเท่ากัน

$$x + 2 = -x + 8$$

ย้าย $-x$ ไปฝั่งซ้ายด้วยการบวก $x$ ทั้งสองข้าง
$$2x + 2 = 8$$

ลบ $2$ ทั้งสองข้าง
$$2x = 6$$
$$x = 3$$

หาค่า $y$ โดยแทน $x = 3$ ลงในสมการแรก
$$y = 3 + 2 = 5$$

ตรวจกับสมการที่สองด้วย
$$y = -3 + 8 = 5$$
ได้ $5$ เท่ากัน ยืนยันว่าจุด $(3, 5)$ อยู่บนทั้งสองเส้นจริง

**จำไว้: หาจุดตัดของกราฟที่เขียนในรูป $y$ เท่ากับอะไร ให้จับฝั่งขวาเท่ากันได้เลย ไม่ต้องจัดรูปอะไรก่อน เพราะที่จุดตัดค่า $y$ ของสองเส้นต้องตรงกันพอดี**

**ทำไมตัวเลือกอื่นผิด:**
ตัวเลือก 1 ($(5, 3)$) ผิดเพราะสลับพิกัด $x$ กับ $y$ แทนดูจะได้ $y = 7$ ไม่ใช่ $3$
ตัวเลือก 2 ($(2, 4)$) ผิดเพราะย้ายข้างพลาด ได้ $x = 2$ ทั้งที่ต้องเป็น $3$
ตัวเลือก 4 ($(4, 6)$) ผิดเพราะแทนในสมการแรกได้ $6$ จริง แต่สมการที่สองได้ $4$ ไม่ตรงกัน ไม่ใช่จุดตัด""",
    T(GRAPH, 'เข้าใจ', 'กลาง'))

# ── 74 ── ci=1
a, b = S(E(Y, 3 * X - 4), E(Y, X + 2)); assert (a, b) == (3, 5)
add(74, 1, r'กราฟของ $y = 3x - 4$ และ $y = x + 2$ ตัดกันที่จุดซึ่งมีพิกัด $x$ เท่ากับเท่าใด',
    [N(a), r'$2$', N(b), r'$6$'], N(a),
    [(2, 'ย้ายข้างพลาดเหลือสองเอ็กซ์เท่ากับสี่'), (3, 'ตอบพิกัด y แทนพิกัด x'), (4, 'บวกสี่กับสองแล้วตอบเลย')],
    r"""**คำตอบ: ข้อ 1.**

ที่จุดตัด ค่า $y$ ของทั้งสองเส้นต้องเท่ากัน จับฝั่งขวามาเท่ากัน

$$3x - 4 = x + 2$$

ย้าย $x$ ไปฝั่งซ้ายด้วยการลบ $x$ ทั้งสองข้าง
$$2x - 4 = 2$$

บวก $4$ ทั้งสองข้าง
$$2x = 6$$

หารด้วย $2$
$$x = 3$$

โจทย์ถามแค่พิกัด $x$ จึงตอบ $3$ ได้เลย ไม่ต้องหา $y$ ต่อ

แต่ถ้าอยากตรวจให้แน่ใจ แทน $x = 3$ ลงทั้งสองสมการ สมการแรกได้ $3(3) - 4 = 5$ สมการที่สองได้ $3 + 2 = 5$ ตรงกันพอดี แปลว่าจุดตัดคือ $(3, 5)$ และพิกัด $x$ เป็น $3$ จริง

**เคล็ดของครูฮีม: โจทย์ถามแค่พิกัดเดียว ก็หาแค่พิกัดนั้นพอ ไม่ต้องแก้ให้ครบทั้งคู่ ประหยัดเวลาในห้องสอบไปได้หลายข้อ แต่ถ้าเหลือเวลา ให้แทนกลับตรวจสักครั้งจะมั่นใจกว่า**

**ทำไมตัวเลือกอื่นผิด:**
ตัวเลือก 2 ($2$) ผิดเพราะย้ายข้างพลาด ได้ $2x = 4$ แทนที่จะเป็น $6$
ตัวเลือก 3 ($5$) ผิดเพราะเป็นพิกัด $y$ ของจุดตัด ไม่ใช่พิกัด $x$ ที่โจทย์ถาม
ตัวเลือก 4 ($6$) ผิดเพราะทำถึง $2x = 6$ แล้วหยุด ลืมหารด้วย $2$""",
    T(GRAPH, 'เข้าใจ', 'กลาง'))

# ── 75 ── ci=3
a, b = S(E(2 * X + Y, 8), E(X - Y, 1)); assert (a, b) == (3, 2)
add(75, 3, r'เส้นตรง $2x + y = 8$ และ $x - y = 1$ ตัดกันที่จุดใด',
    [r'$(2, 3)$', r'$(1, 6)$', P(a, b), r'$(4, 0)$'], P(a, b),
    [(1, 'สลับพิกัด x กับ y'), (2, 'ตรวจแค่สมการแรก'), (4, 'ตรวจแค่สมการเดียวแล้วเดา')],
    r"""**คำตอบ: ข้อ 3.**

จุดตัดของเส้นตรงสองเส้น คือคำตอบของระบบสมการที่ประกอบด้วยสองสมการนั้น เป็นเรื่องเดียวกันเลย ครูฮีมจึงแก้ระบบตามปกติ

สัมประสิทธิ์ของ $y$ เป็น $+1$ กับ $-1$ ตรงข้ามกันพอดี บวกสมการได้เลย
$$(2x + y) + (x - y) = 8 + 1$$
$$3x = 9$$
$$x = 3$$

แทนกลับเข้าสมการที่สอง
$$3 - y = 1$$
$$y = 2$$

ตรวจกลับกับสมการแรก
$$2(3) + 2 = 6 + 2 = 8$$
ตรงกับฝั่งขวา จุดตัดคือ $(3, 2)$

ครูฮีมอยากให้เห็นภาพว่าสองเรื่องนี้เป็นเรื่องเดียวกัน เวลาแก้ระบบสมการด้วยพีชคณิต เรากำลังหาจุดที่กราฟสองเส้นมาเจอกันนั่นเอง ระบบที่ไม่มีคำตอบก็คือกราฟที่ไม่มีจุดตัด และระบบที่มีคำตอบมากมายก็คือกราฟที่ทับกันสนิท

**จำไว้: คำตอบของระบบสมการ กับ จุดตัดของกราฟสองเส้น เป็นสิ่งเดียวกัน โจทย์ถามอย่างไหนก็แก้ระบบเหมือนกันหมด**

**ทำไมตัวเลือกอื่นผิด:**
ตัวเลือก 1 ($(2, 3)$) ผิดเพราะสลับพิกัด แทนดูจะได้ $2 - 3 = -1$ ไม่ใช่ $1$
ตัวเลือก 2 ($(1, 6)$) ผิดเพราะสมการแรกจริงคือ $2 + 6 = 8$ แต่สมการที่สองได้ $1 - 6 = -5$ ไม่ใช่ $1$
ตัวเลือก 4 ($(4, 0)$) ผิดเพราะสมการที่สองจริงคือ $4 - 0 = 4$ ซึ่งก็ไม่ใช่ $1$ อยู่ดี ตรวจไม่ผ่านทั้งคู่""",
    T(GRAPH, 'คิดเลข', 'กลาง'))

# ── 76 ── ci=2
px, py = 2, 3
assert holds(E(X + Y, 5), px, py) and holds(E(2 * X - Y, 1), px, py)
assert not holds(E(X + Y, 6), px, py)
assert not (holds(E(3 * X - Y, 3), px, py) and holds(E(X + 2 * Y, 7), px, py))
assert not (holds(E(2 * X + Y, 7), px, py) and holds(E(X - Y, 1), px, py))
add(76, 2, r'กราฟของระบบสมการในข้อใดตัดกันที่จุด $(2, 3)$',
    [r'$x + y = 6$ และ $x - y = -1$',
     r'$x + y = 5$ และ $2x - y = 1$',
     r'$3x - y = 3$ และ $x + 2y = 7$',
     r'$2x + y = 7$ และ $x - y = 1$'],
    r'$x + y = 5$ และ $2x - y = 1$',
    [(1, 'ตรวจเฉพาะสมการที่สอง'), (3, 'ตรวจเฉพาะสมการแรก'), (4, 'ตรวจเฉพาะสมการแรกแล้วเดาต่อ')],
    r"""**คำตอบ: ข้อ 2.**

จุด $(2, 3)$ จะเป็นจุดตัดของระบบใด ก็ต่อเมื่อแทน $x = 2$ และ $y = 3$ ลงไปแล้วสมการ **ทั้งสอง** เป็นจริงพร้อมกัน ครูฮีมตรวจทีละข้อโดยไม่ข้ามสมการไหนเลย

ตรวจข้อ 2 สมการแรก
$$2 + 3 = 5$$
ตรงกับฝั่งขวา จริง

สมการที่สอง
$$2(2) - 3 = 4 - 3 = 1$$
ตรงกับฝั่งขวา จริงเช่นกัน ทั้งคู่จริงพร้อมกัน จุด $(2, 3)$ จึงเป็นจุดตัดของระบบนี้

ครูฮีมชี้กับดักที่ข้อสอบชอบวาง ตัวลวงทุกตัวจะมีสมการหนึ่งที่จริง เพื่อล่อให้เราเห็นแล้วรีบตอบ ต้องตรวจให้ครบทั้งสองบรรทัดเสมอ ตรวจบรรทัดเดียวแล้วตอบคือแพ้กับดักนี้ทันที

**เคล็ดของครูฮีม: ตรวจตัวเลือกแบบนี้ ให้ตรวจสมการที่สองก่อน เพราะข้อสอบมักทำสมการแรกให้จริงเป็นเหยื่อล่อ ถ้าสมการที่สองไม่ผ่าน ตัดทิ้งได้เลยโดยไม่ต้องเสียเวลาตรวจสมการแรก**

**ทำไมตัวเลือกอื่นผิด:**
ตัวเลือก 1 ผิดเพราะสมการที่สองจริงคือ $2 - 3 = -1$ แต่สมการแรกได้ $2 + 3 = 5$ ไม่ใช่ $6$
ตัวเลือก 3 ผิดเพราะสมการแรกจริงคือ $3(2) - 3 = 3$ แต่สมการที่สองได้ $2 + 6 = 8$ ไม่ใช่ $7$
ตัวเลือก 4 ผิดเพราะสมการแรกจริงคือ $2(2) + 3 = 7$ แต่สมการที่สองได้ $2 - 3 = -1$ ไม่ใช่ $1$""",
    T(GRAPH, 'เข้าใจ', 'ยาก'))

# ── 77 ── ci=1
yc = sp.solve(sp.Eq(3 * 0 - 2 * Y, 12), Y)[0]; assert yc == -6
add(77, 1, r'กราฟของสมการ $3x - 2y = 12$ ตัดแกน $Y$ ที่จุดใด',
    [P(0, yc), r'$(0, 6)$', r'$(4, 0)$', r'$(-4, 0)$'], P(0, yc),
    [(2, 'ลืมเครื่องหมายลบตอนหาร'), (3, 'ตอบจุดตัดแกน X แทน'), (4, 'ผิดทั้งแกนและเครื่องหมาย')],
    r"""**คำตอบ: ข้อ 1.**

หาจุดตัดแกน $Y$ ให้แทน $x = 0$

$$3(0) - 2y = 12$$
$$-2y = 12$$

หารด้วย $-2$ บวกหารลบได้ลบ
$$y = -6$$

จุดตัดแกน $Y$ จึงเป็น $(0, -6)$

หาจุดตัดแกน $X$ ไว้เทียบด้วย แทน $y = 0$
$$3x = 12$$
$$x = 4$$
จุดตัดแกน $X$ คือ $(4, 0)$

ครูฮีมเตือนจุดพลาดของข้อนี้ ค่าคงตัว $12$ อยู่ฝั่งขวาก็จริง แต่ไม่ใช่พิกัดของจุดตัดแกน $Y$ โดยตรง เพราะสัมประสิทธิ์ของ $y$ ไม่ใช่ $1$ ต้องหารด้วย $-2$ ก่อน และเครื่องหมายลบทำให้ค่ากลายเป็นลบด้วย

ถ้าอยากอ่านจุดตัดแกน $Y$ ได้ทันที ให้จัดรูปเป็น $y$ เท่ากับอะไรก่อน
$$y = \frac{3x - 12}{2}$$
แทน $x = 0$ ก็เห็นทันทีว่าได้ $-6$

**จำไว้: ค่าคงตัวฝั่งขวาไม่ใช่จุดตัดแกน $Y$ โดยตรง ยกเว้นเมื่อสัมประสิทธิ์ของ $y$ เป็น $1$ พอดี กรณีอื่นต้องหารก่อนเสมอ และอย่าทิ้งเครื่องหมายลบ**

**ทำไมตัวเลือกอื่นผิด:**
ตัวเลือก 2 ($(0, 6)$) ผิดเพราะหาร $12$ ด้วย $2$ แล้วทิ้งเครื่องหมายลบไป
ตัวเลือก 3 ($(4, 0)$) ผิดเพราะนั่นคือจุดตัดแกน $X$ ไม่ใช่แกน $Y$ ตามที่โจทย์ถาม
ตัวเลือก 4 ($(-4, 0)$) ผิดทั้งสองจุด คือทั้งผิดแกนและเติมเครื่องหมายลบผิดที่""",
    T(GRAPH, 'เข้าใจ', 'ยาก'))

# ── 78 ── ci=3
k = sp.Symbol('k')
kv = sp.solve(sp.Eq(3, 2 * k - 5), k)[0]; assert kv == 4
add(78, 3, r'ถ้าเส้นตรง $y = 2x - 5$ ผ่านจุด $(k, 3)$ แล้ว $k$ มีค่าเท่าใด',
    [r'$1$', r'$2$', N(kv), r'$8$'], N(kv),
    [(1, 'ลบห้าแทนที่จะบวกตอนย้ายข้าง'), (2, 'ลืมหารด้วยสัมประสิทธิ์'), (4, 'บวกแล้วไม่หาร')],
    r"""**คำตอบ: ข้อ 3.**

จุด $(k, 3)$ แปลว่าพิกัด $x$ คือ $k$ ซึ่งยังไม่รู้ค่า ส่วนพิกัด $y$ คือ $3$ เมื่อโจทย์บอกว่าเส้นตรงผ่านจุดนี้ แปลว่าแทนลงในสมการแล้วต้องเป็นจริง

แทน $x = k$ และ $y = 3$
$$3 = 2k - 5$$

ย้าย $-5$ ไปฝั่งซ้ายด้วยการบวก $5$ ทั้งสองข้าง
$$3 + 5 = 2k$$
$$8 = 2k$$

หารด้วย $2$
$$k = 4$$

ตรวจกลับ แทน $x = 4$ ลงในสมการ
$$y = 2(4) - 5 = 8 - 5 = 3$$
ได้ $y = 3$ ตรงกับพิกัดที่โจทย์ให้ ยืนยันว่าจุด $(4, 3)$ อยู่บนเส้นตรงนี้จริง

**เคล็ดของครูฮีม: โจทย์ที่ให้จุดมีตัวอักษรแทรก ให้ดูก่อนว่าตัวอักษรอยู่ตำแหน่งไหนของคู่อันดับ ตัวหน้าคือ $x$ ตัวหลังคือ $y$ แทนสลับที่เมื่อไหร่ ได้คำตอบผิดทันทีทั้งที่วิธีคิดถูกหมด**

**ทำไมตัวเลือกอื่นผิด:**
ตัวเลือก 1 ($1$) ผิดเพราะย้ายข้างพลาด เอา $5$ ไปลบแทนที่จะบวก ได้ $2k = -2$
ตัวเลือก 2 ($2$) ผิดเพราะแทนสลับที่ ใช้ $x = 3$ แล้วหา $y$ แทนที่จะทำกลับกัน
ตัวเลือก 4 ($8$) ผิดเพราะทำถึง $2k = 8$ แล้วหยุด ลืมหารด้วย $2$""",
    T(GRAPH, 'เข้าใจ', 'กลาง'))

dump('t9N7gu06nIEy8GdE3Dmj', os.path.join(os.path.dirname(os.path.dirname(os.path.abspath(__file__))), 'out-m3-sys-F.json'))
