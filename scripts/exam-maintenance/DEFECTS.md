# ข้อบกพร่องที่เจอระหว่างติด tag 4 มิติ

บันทึกไว้ระหว่างกวาด tag ทั้งคลัง — **แก้ครบทั้ง 3 รายการแล้ว 2026-08-27**
ใช้ไปป์ไลน์เดียวกับ dedup: `scripts/exam-maintenance/dedup/kit.py` + `scripts/apply-dedup.js`

---

## ✅ 1. อสมการเชิงเส้นตัวแปรเดียว ม.3 — `ioBGz4wkXQNopR46ksT1` (แก้แล้ว)

**ปัญหา:** ข้อ 1-13 และ 26-40 (รวม **28 ข้อ**) เป็นโจทย์ "หารพหุนามด้วยเอกนาม" ล้วน เช่น
`ผลหารของ 8x²/2x`, `(15x⁴y³ − 9x²y² + 6xy)/3xy` — ไม่มีเครื่องหมายอสมการเลยสักข้อ
เป็นเนื้อหา ม.2 บทพหุนาม ไม่ใช่บทอสมการ

**แก้ไข:** เขียนโจทย์อสมการใหม่ครบ 28 ข้อ คง `correctIndex` เดิมทุกข้อ
- `dedup/sets/m3-ineq-A.py` ข้อ 1-13 — ความหมาย/สมบัติ, แก้อสมการพื้นฐาน, จำนวนเต็มที่สอดคล้อง, แปลประโยค
- `dedup/sets/m3-ineq-B.py` ข้อ 26-40 — สองขั้นตอน, กระจายวงเล็บ, ตัวแปรสองข้าง, เศษส่วน/ทศนิยม, อสมการสองชั้น

ทุกข้อแก้สดด้วย SymPy โดย **เทียบเซตคำตอบ** (`relational=False` + `expected.as_set()`)
ไม่เทียบรูปประโยค เพราะ SymPy คืน `5 < x` ซึ่งไม่ `==` กับ `x > 5`

ตอนนี้ทั้งชุด 250 ข้อเป็นอสมการล้วน · หัวข้อย่อย 8 หัวข้อ · doc 776,328 ไบต์ (74.0%)

---

## ✅ 2. ระบบสมการเชิงเส้นสองตัวแปร ม.3 — `t9N7gu06nIEy8GdE3Dmj` (แก้แล้ว)

**ปัญหา 1:** ข้อ 12-90 และ 101-110 (รวม **89 ข้อ**) เป็นโจทย์ปัญหา **สมการกำลังสอง** เช่น
ผลคูณจำนวนเรียงติดกัน, สามเหลี่ยมมุมฉากด้วยพีทาโกรัส, โยนลูกบอล $h(t)$,
ท่อน้ำเติมถัง, เหมารถหารกันคนละเท่าๆ กัน — แก้ด้วยระบบสมการเชิงเส้นไม่ได้

**ปัญหา 2:** ทั้งชุด **ไม่มีข้อแก้ระบบสมการแบบพีชคณิตล้วนเลย** เป็นโจทย์ปัญหา 100%
เด็กที่ยังตั้งสมการไม่เป็นจะไม่มีข้อให้ฝึกวิธีแก้

**แก้ไข:** เขียนใหม่ครบ 89 ข้อ คง `correctIndex` เดิมทุกข้อ และ**ใช้โอกาสนี้อุดช่องว่างข้อ 2 ไปด้วย**
ทั้ง 89 ข้อใหม่เป็นพีชคณิตล้วน ไม่มีโจทย์ปัญหาเพิ่ม (ของเดิมมีโจทย์ปัญหา 161 ข้ออยู่แล้ว)

| ไฟล์ | ข้อ | เนื้อหา |
|---|---|---|
| `dedup/sets/m3-sys-A.py` | 12-19 | ความหมายและการตรวจคำตอบของระบบสมการ |
| `dedup/sets/m3-sys-B.py` | 20-34 | วิธีแทนค่า |
| `dedup/sets/m3-sys-C.py` | 35-52 | วิธีกำจัดตัวแปร |
| `dedup/sets/m3-sys-D.py` | 53-62 | เศษส่วนและทศนิยม |
| `dedup/sets/m3-sys-E.py` | 63-70 | จำนวนคำตอบ (ตัดกัน/ขนาน/ทับกัน) |
| `dedup/sets/m3-sys-F.py` | 71-78 | กราฟและจุดตัด |
| `dedup/sets/m3-sys-G.py` | 79-90 | หาค่าคงตัวจากเงื่อนไขคำตอบ + จัดรูปก่อนแก้ |
| `dedup/sets/m3-sys-H.py` | 101-110 | บล็อกยาก/ผสม (เส้นสามเส้นผ่านจุดเดียว, แทนก้อนเป็นตัวแปร, ผลต่างกำลังสอง) |

ตัวช่วยกลาง `dedup/syskit.py` — `S()` แก้ระบบสดด้วย SymPy ยืนยันว่ามีคำตอบเดียวจริง,
`N()` บังคับให้คำตอบเป็นจำนวนเต็ม, `holds()` ตรวจว่าคู่อันดับสอดคล้องกับสมการ

ตอนนี้ทั้งชุด 250 ข้อ = โจทย์ปัญหา 161 + พีชคณิตล้วน 89 · หัวข้อย่อย 16 หัวข้อ ·
doc 865,951 ไบต์ (82.6% — เล็กลงจากเดิม 929,754)

> ⚠️ **บั๊กที่เกือบหลุด:** รอบแรก `syskit.N()` คืนเลขเปล่า ทำให้ตัวเลือกในข้อเดียวกันหน้าตาคนละแบบ
> เลขเปล่า `21` ปนกับ LaTeX `$49$` — และบังเอิญ **ทั้ง 68 ข้อที่ปน คำตอบถูกอยู่ในกลุ่มเลขเปล่าหมด**
> กลายเป็นใบ้คำตอบให้เด็กเดาถูกจากหน้าตาตัวอักษร แก้แล้วโดยให้ `N()` ครอบ `$` เสมอ
> **กติกาใหม่: ตัวเลือกในข้อเดียวกันต้องจัดรูปแบบเดียวกันทั้งสี่ตัว**

**89 ข้อเดิมเก็บไว้ครบที่ `scripts/exam-maintenance/rescued/m3-quadratic-wordproblems-89.json`**
(โจทย์ + ตัวเลือก + correctIndex + เฉลย) ถ้าครูฮีมอยากได้ **"ชุดกำลังสอง ชุดที่ 2"**
ยกไฟล์นี้ไปตั้งเป็นชุดใหม่ได้เลย ตรวจแล้วไม่ซ้ำกับชุด `0iPY5ChZDscVnhBQbXXH` สักข้อ
เป็นโจทย์ดี แค่เคยอยู่ผิดบท

> ⚠️ **กับดัก:** `apply-dedup.js` เขียนทับ `scripts/tmp/dedup/backup/<examId>.json` ใหม่ทุกครั้งที่รัน --apply
> รันรอบสองเมื่อไหร่ ของเดิมหายทันที รอบนี้กู้กลับมาได้จาก `tagging/unlabeled.json` ที่ export ไว้ก่อนหน้า
> **ถ้าจะแก้ชุดไหนซ้ำสองรอบ ให้ก๊อปไฟล์ backup ออกไปเก็บที่อื่นก่อนเสมอ**

---

## ✅ 3. แบบฝึกหัด สอบเข้า ม.1 ชุดที่ 7 — `x7gseHZqPh0KMXaSyEni` ข้อ 94 (แก้แล้ว)

**ปัญหา:** โจทย์ขึ้นต้นด้วยข้อความภาษาอังกฤษที่หลุดมาจากคำสั่งสร้างรูป
`[Image comparing a small pizza and a large pizza twice its radius] ร้านพิซ...`
ผิดกติกา "ห้ามใช้ภาษาอังกฤษในเนื้อหาที่ส่งนักเรียน"

**แก้ไข:** `scripts/tmp/fix-en-leak-q94.js` — ตัดเฉพาะวงเล็บเหลี่ยมต้นโจทย์ที่มีตัวอักษรอังกฤษข้างใน
แก้ 1 ข้อพอดี (รุ่นแรกที่ไม่บังคับว่าต้องมีตัวอักษรอังกฤษ ไปโดน 23 ข้อ ซึ่ง 22 ข้อต่างกันแค่ช่องว่างนำ)
สแกนซ้ำทั้งคลังแล้ว **ไม่เหลือภาษาอังกฤษหลุดแบบนี้ที่อื่นอีก**

---

## ⏳ 4. การกระจายตำแหน่งคำตอบเอียง — 27 ชุด (รอครูฮีมเคาะ)

> **อัปเดต 2026-08-27:** สามชุดที่หนักที่สุด (สอบเข้า ม.1 ชุด 1/3/4 และชุด 2)
> **ไม่ได้เอียงเพราะออกแบบ แต่เอียงเพราะเฉลยชี้ผิดตัว** — ดูข้อ 5 ด้านล่าง แก้แล้ว
> ตัวเลขในตารางข้างล่างนี้เป็นของ**ก่อนแก้** เก็บไว้ให้เห็นที่มา
> ที่เหลืออีก 14 ชุดยังเป็นการเอียงจริง รอครูฮีมเคาะเหมือนเดิม

สแกน 88 ชุดที่มี ≥40 ข้อ (18,120 ข้อ) วัดด้วยไคสแควร์เทียบกับ 25/25/25/25
รายงานเต็มรายชุด: `scripts/exam-maintenance/answer-distribution-20260827.json`

ทั้งคลังรวมกันกระจายพอใช้ (ก 25.4% · ข 26.5% · ค 26.2% · ง 21.8%) แต่รายชุดเอียงหนัก

**หนักที่สุด 3 ชุด มีตัวเลือกที่ไม่เคยเป็นคำตอบเลยแม้แต่ข้อเดียว**

| ชุด | ก/ข/ค/ง | เดาตัวเดียวรวดได้ |
|---|---|---|
| สอบเข้า ม.1 ชุดที่ 3 | 63 / **0** / 24 / 11 | ตอบ ก รวด ได้ **64%** |
| สอบเข้า ม.1 ชุดที่ 4 | 52 / **0** / 33 / 9 | ตอบ ก รวด ได้ **55%** |
| สอบเข้า ม.1 ชุดที่ 1 | 41 / 30 / 29 / **0** | ตอบ ก รวด ได้ 41% |

อีก 14 ชุดเอียงระดับ p<.001 เช่น ม.1 กราฟฯ (65/108/65/**12**) · ม.2 จำนวนจริง (80/98/56/**15**) ·
ม.2 พีทาโกรัส (65/87/81/**17**) · ม.2 พหุนาม (**108**/43/51/48) · ม.3 อสมการ (93/62/61/34)
รวมเอียงพอควรอีก 10 ชุด · กระจายดี 61 ชุด

**ทำไมสำคัญ:** เด็กที่จับทางได้ว่า "ชุดนี้ไม่เคยตอบ ข" จะตัดตัวเลือกทิ้งฟรีหนึ่งตัวทุกข้อ
และชุดที่ตอบ ก 64% เดารวดก็ได้เกินครึ่งโดยไม่ต้องคิดเลข คะแนนที่ได้ไม่สะท้อนความสามารถจริง
กระทบทั้งความน่าเชื่อถือของคลัง และระบบวิเคราะห์จุดอ่อนที่อ่านจากคำตอบของเด็ก

**ทำไมผมไม่แก้ให้เลย:** แก้ต้อง "สลับตำแหน่งตัวเลือก" ซึ่งลากไปถึงสามอย่างพร้อมกัน
1. `correctIndex` เปลี่ยน → ผลสอบเก่าที่เก็บเป็นเลขตัวเลือกไว้จะชี้ผิดตัว (เช่น สมุดข้อผิดของเด็ก)
2. หัวเฉลย `**คำตอบ: ข้อ N.**` ต้องเลขใหม่
3. เฉลยอ้างตัวเลือกเป็นร้อยแบบ ทั้ง `ตัวเลือก 1 (...) ผิดเพราะ` และประโยคลอยอย่าง
   `ถ้าตอบ 7 (ตัวเลือก 1) แสดงว่า...` — ทำอัตโนมัติแล้วมีโอกาสได้เฉลยที่ชี้ผิดตัว
   ซึ่ง **แย่กว่าการกระจายเอียง** เพราะเด็กอ่านแล้วเข้าใจผิดไปเลย

**ทางเลือกที่เสนอ**
1. แก้เฉพาะ 3 ชุดที่มีตัวเลือกไม่เคยใช้ (สอบเข้า ม.1 ชุด 1/3/4 รวม ~300 ข้อ) ทำมือ ตรวจทีละข้อ — เสี่ยงต่ำสุด คุ้มสุด
2. แก้ทั้ง 17 ชุดระดับ p<.001 (~4,000 ข้อ) ต้องสร้างเครื่องมือ remap + ตรวจเฉลยด้วยคนอีกรอบ
3. ไม่ย้อนแก้ของเก่า แต่บังคับกติกากับของใหม่ทุกชุดตั้งแต่นี้ไป (`apply-dedup.js` พิมพ์การกระจายให้ดูอยู่แล้ว)


---

## ✅ 5. เฉลยชี้ผิดตัว 171 ข้อ — เด็กตอบถูกแล้วโดนตัดว่าผิด (แก้แล้ว 2026-08-27)

**นี่คือต้นเหตุจริงของข้อ 4** ไม่ใช่การออกแบบเอียง

`components/exam/ExamSystem.tsx:588` ตัดสินถูกผิดจาก `ans === q.correctIndex`
แต่ในชุดสอบเข้า ม.1 ชุดที่ 1-4 ค่า `correctIndex` เพี้ยน เฉลยเขียนว่าตอบ ข แต่ระบบเก็บว่าตอบ ก

| ชุด | แก้ | การกระจายก่อน → หลัง |
|---|---|---|
| ชุดที่ 1 | 40 ข้อ | `41/30/29/0` → `15/51/29/5` |
| ชุดที่ 2 | 50 ข้อ + เขียนเฉลยใหม่ 10 ข้อที่ไม่มีเฉลยเลย | `50/14/30/6` → `14/43/33/10` |
| ชุดที่ 3 | 47 ข้อ + เขียนโจทย์/เฉลยใหม่ 1 ข้อ | `63/0/24/11` → `21/46/20/11` |
| ชุดที่ 4 | 34 ข้อ + เขียนเฉลยใหม่ 5 ข้อ | `52/0/33/9` → `24/29/32/9` |

รายละเอียดและเครื่องมือ: `scripts/exam-maintenance/answerfix/README.md`

### ⏳ 5.1 คะแนนของเด็กที่ทำไปแล้วยังผิดอยู่ (รอครูฮีมตัดสิน)

**นี่คือส่วนที่ยังไม่ได้แก้** รายละเอียดรายคน: `answerfix/impact-on-students-20260827.json`

มีผลสอบเก็บไว้ **86 ครั้ง** บนสี่ชุดนี้ · **75 ครั้งคะแนนผิด**
(ได้น้อยกว่าความจริง **70 ครั้ง** · ได้มากกว่าความจริง 5 ครั้ง)

หนักที่สุดคือเด็กที่ทำครบ 100 ข้อ ระบบให้ **38 คะแนน ทั้งที่ตอบถูกจริง 80 คะแนน**
อีกคน **56 → 95** · อีกคน **54 → 93** · เฉพาะคนที่ทำเกิน 10 ข้อ 57 ราย คะแนนรวม **1,181 ควรเป็น 2,031**

**ทำไมยังไม่แก้ให้:** ผลสอบเก็บคำตอบดิบไว้ในฟิลด์ `answers` จึงคิดคะแนนใหม่ได้ตรงเป๊ะ
แต่ในเอกสารเดียวกันยังมีฟิลด์ที่ **แอปคำนวณต่อ** อีกหลายตัว — `grade`, `percent`, `bestPercent`,
`history`, `wrongQuestions`, `topicStats`, `percentileHistory` ถ้าคิดใหม่ไม่ครบทุกตัวให้ตรงกับสูตรในแอป
จะได้เอกสารที่ **ข้างในขัดกันเอง** ซึ่งแย่กว่าผิดแบบสม่ำเสมออย่างตอนนี้
และนี่เป็นข้อมูลผลสอบของเด็ก ควรให้ครูฮีมเป็นคนตัดสิน

**ทางเลือก**
1. ไม่ยุ่งกับของเก่า แต่แจ้งเด็กให้ทำใหม่ — ตอนนี้เฉลยถูกแล้ว ทำใหม่ได้คะแนนจริงทันที (ง่ายและปลอดภัยที่สุด)
2. คิดคะแนนใหม่ให้ครบทุกฟิลด์ ต้องไล่สูตรใน `ExamSystem.tsx` ให้ตรงก่อน แล้วเขียนทับ (มีสำรองไว้แล้ว)
3. ลบผลสอบของสี่ชุดนี้ทิ้ง ให้ทุกคนเริ่มใหม่หมด


---

## ⏳ 6. เกลี่ยตำแหน่งคำตอบ 13 ชุด — เครื่องมือพร้อม รอสั่งรัน

หลังแก้ข้อ 5 แล้ว ชุดที่ยังเอียงจริงเหลือ 13 ชุด สร้างเครื่องมือเกลี่ยไว้แล้วที่
`scripts/exam-maintenance/rebalance/` (มี README อธิบายเงื่อนไขความปลอดภัยครบ)

dry-run ผ่านสะอาด: **13 ชุด · สลับ 336 ข้อ · ตรวจไม่ผ่าน 0 ข้อ**
หลายชุดจากเอียงหนักเป็นสมดุลพอดี เช่น ม.1 กราฟฯ `65/108/65/12` → `62/63/63/62`

สลับแค่สองตำแหน่งต่อข้อ และแตะเฉพาะข้อที่ **เฉลยไม่ได้อ้างถึงตัวเลือกด้วยตำแหน่ง**
กับ **ตัวเลือกไม่ได้เรียงลำดับตัวเลขอยู่แล้ว** ข้อที่เฉลยอ้างตำแหน่ง (เช่น ม.2 พหุนาม 235 จาก 250 ข้อ)
ไม่แตะเลย เพราะสลับแล้วเฉลยจะชี้ผิดตัว ซึ่งแย่กว่าปล่อยให้เอียง

**สั่งรันได้ด้วย** (dry-run ก่อน แล้วค่อยเติม `--apply` แล้วปิดท้ายด้วย bust-caches)

```
node scripts/exam-maintenance/rebalance/rebalance.js
node scripts/exam-maintenance/rebalance/rebalance.js --apply
node scripts/bust-caches.js exams
```
