"use client";
// สถิติคลังข้อสอบ — AdminGuard + AdminShell มาจาก app/admin/layout.tsx อัตโนมัติ
import ExamStatsBoard from "@/components/admin/ExamStatsBoard";

export default function ExamStatsPage() {
    return <ExamStatsBoard />;
}
